PORT=8080
DRY_RUN=true
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Add MONDAY_API_TOKEN only in an approved server-side secret manager.
# Never place it in storefront JavaScript, Miva templates, or Git.
MONDAY_API_TOKEN=
